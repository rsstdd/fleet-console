{
  "extends": "../../tsconfig.base.json",
  "_comment": "Node-side library. No DOM lib: this package must stay renderer-independent (Principle 9). Consumers import the TypeScript source directly, which is why the base config sets erasableSyntaxOnly. resolveJsonModule is on because recorded vendor payloads stay JSON — the bytes the wire carries — and the ./testing subpath imports them statically rather than reading the filesystem (ADR 11).",
  "compilerOptions": {
    "lib": ["ES2023"],
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "types": ["node"],
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.tsbuildinfo",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src", "vitest.config.ts", "eslint.config.js"]
}
