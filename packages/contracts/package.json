{
  "name": "@fleet/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Canonical robot envelope, capability contracts, wire schemas, and pure freshness derivation. The bottom of the dependency graph.",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
