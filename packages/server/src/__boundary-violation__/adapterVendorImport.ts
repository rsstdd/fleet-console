// @ts-nocheck -- the forbidden private subpath deliberately does not resolve.
/** Deliberate adapter-vendor deep import used only by enforcement.test.ts. */
import "@fleet/adapters/vendors/a/adapter";
