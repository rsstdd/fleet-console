// @ts-nocheck -- production code must not resolve the test-only subpath at all.
/** Deliberate production import of the fixture subpath, used only by enforcement.test.ts. */
import "@fleet/adapters/testing";
