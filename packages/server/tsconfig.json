{
  "extends": "../../tsconfig.base.json",
  "_comment": "Node runtime package. No DOM lib: this package must never reproduce client presentation logic (AGENTS.md § Dependency and ownership boundaries). Consumers import the TypeScript source directly, which is why the base config sets erasableSyntaxOnly.",
  "compilerOptions": {
    "lib": ["ES2023"],
    "allowImportingTsExtensions": true,
    "types": ["node"],
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.tsbuildinfo",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src", "vitest.config.ts", "eslint.config.js"]
}
