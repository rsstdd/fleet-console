{
  "rules": {
    "color-no-hex": true,
    "declaration-property-value-disallowed-list": {
      "/^(color|background|background-color|border-color|fill|stroke)$/": ["/^rgb/", "/^hsl/"],
      "/.*/": ["/-?(?:[0-9]+(?:\\.[0-9]+)?|\\.[0-9]+)(?:px|rem)\\b/i"]
    },
    "selector-class-pattern": [
      "^[a-z][a-zA-Z0-9]*(-[a-z0-9]+)*(__[a-z][a-zA-Z0-9]*)?(--[a-z][a-zA-Z0-9]*(-[a-z0-9]+)*)?$",
      {
        "message": "Class names are BEM: block, block__element, block--modifier. `docs/02_component-specs` writes the markup this stylesheet has to match, and it uses `__` elements \u2014 a pattern that rejects them forces a local suppression on every rule that styles one, which is the workaround `packages/FIXME.md` F8 objected to."
      }
    ]
  },
  "overrides": [
    {
      "files": ["**/tokens.css"],
      "rules": {
        "color-no-hex": null,
        "declaration-property-value-disallowed-list": null
      }
    }
  ]
}
